apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: survivor-apps
  labels:
    app: chatbot-service
spec:
  selector:
    app: chatbot-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP